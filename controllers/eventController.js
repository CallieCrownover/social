const db = require("../models");

module.exports = {
    const showAllEvent = async function() {
        const user = await Identifier.find().populate("customer");
      
        console.log("> All Identifiers\n", identifiers);
      }
}